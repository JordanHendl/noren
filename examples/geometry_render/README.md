# [geometry_render]

Loads untextured geometry from the database and renders it to the display. Run
with `cargo run --example geometry_render [primitive]` to select a default
primitive (defaults to `sphere`). Supported primitive arguments are `sphere`,
`cube`, `quad`, `plane`, `cylinder`, and `cone` (the `geometry/` prefix is
optional).
